# 轻写 - AI辅助小说创作平台 前后端分离迁移计划

## 📋 项目概述

**原架构**：Next.js 全栈框架（前后端耦合）  
**目标架构**：React 前端 + Python FastAPI 后端（前后端分离）  
**数据库**：MySQL 8.0  
**预计工期**：5-7天

---

## 🎯 技术栈选型

### 前端技术栈
```
├── React 19.0.0
├── Vite 6.x (构建工具)
├── React Router 6.x (路由管理)
├── Axios (HTTP客户端)
├── TailwindCSS 4.x (样式框架)
├── Shadcn/ui (UI组件库 - 保持现有)
├── Zustand (状态管理)
├── React Query (数据获取和缓存)
├── Socket.io-client (WebSocket实时通信)
└── React Hook Form + Zod (表单验证)
```

### 后端技术栈
```
├── Python 3.10+
├── FastAPI 0.115+ (Web框架)
├── SQLAlchemy 2.0+ (ORM)
├── Pydantic 2.0+ (数据验证)
├── MySQL 8.0 (数据库)
├── PyMySQL / mysqlclient (MySQL驱动)
├── python-socketio (WebSocket支持)
├── httpx (异步HTTP客户端 - 调用豆包API)
├── python-jose (JWT认证)
├── passlib + bcrypt (密码加密)
├── uvicorn (ASGI服务器)
└── python-dotenv (环境变量管理)
```

---

## 📁 项目目录结构

### 后端项目结构 (backend/)
```
backend/
├── app/
│   ├── __init__.py
│   ├── main.py                 # FastAPI应用入口
│   ├── config.py               # 配置文件
│   ├── database.py             # 数据库连接
│   ├── dependencies.py         # 依赖注入
│   │
│   ├── models/                 # SQLAlchemy模型
│   │   ├── __init__.py
│   │   ├── user.py
│   │   ├── novel.py
│   │   ├── chapter.py
│   │   ├── character.py
│   │   ├── world_building.py
│   │   └── outline.py
│   │
│   ├── schemas/                # Pydantic模式(请求/响应)
│   │   ├── __init__.py
│   │   ├── user.py
│   │   ├── novel.py
│   │   ├── chapter.py
│   │   ├── character.py
│   │   ├── world_building.py
│   │   └── outline.py
│   │
│   ├── api/                    # API路由
│   │   ├── __init__.py
│   │   ├── auth.py             # 认证相关
│   │   ├── novels.py           # 小说管理
│   │   ├── chapters.py         # 章节管理
│   │   ├── characters.py       # 角色管理
│   │   ├── world_building.py   # 世界观管理
│   │   ├── outlines.py         # 大纲管理
│   │   └── ai.py               # AI辅助创作
│   │
│   ├── services/               # 业务逻辑层
│   │   ├── __init__.py
│   │   ├── auth_service.py
│   │   ├── novel_service.py
│   │   ├── ai_service.py       # 豆包AI集成
│   │   └── websocket_service.py
│   │
│   ├── utils/                  # 工具函数
│   │   ├── __init__.py
│   │   ├── security.py         # 密码加密、JWT
│   │   └── helpers.py
│   │
│   └── middleware/             # 中间件
│       ├── __init__.py
│       └── cors.py
│
├── alembic/                    # 数据库迁移
│   ├── versions/
│   └── env.py
│
├── tests/                      # 测试文件
│   ├── __init__.py
│   ├── test_auth.py
│   └── test_novels.py
│
├── .env                        # 环境变量
├── .env.example                # 环境变量示例
├── requirements.txt            # Python依赖
├── alembic.ini                 # Alembic配置
└── README.md                   # 后端说明文档
```

### 前端项目结构 (frontend/)
```
frontend/
├── public/
│   ├── favicon.ico
│   └── logo-64.png
│
├── src/
│   ├── main.tsx                # 应用入口
│   ├── App.tsx                 # 根组件
│   ├── vite-env.d.ts
│   │
│   ├── pages/                  # 页面组件
│   │   ├── Home.tsx            # 首页
│   │   ├── Login.tsx           # 登录页
│   │   ├── Register.tsx        # 注册页
│   │   ├── Dashboard.tsx       # 工作台
│   │   ├── NovelEditor.tsx     # 小说编辑器
│   │   └── ChapterEditor.tsx   # 章节编辑器
│   │
│   ├── components/             # 复用组件(从现有项目迁移)
│   │   ├── ui/                 # Shadcn组件
│   │   ├── layout/
│   │   │   ├── Header.tsx
│   │   │   ├── Sidebar.tsx
│   │   │   └── Footer.tsx
│   │   ├── novel/
│   │   │   ├── NovelCard.tsx
│   │   │   ├── NovelList.tsx
│   │   │   └── NovelForm.tsx
│   │   ├── chapter/
│   │   ├── character/
│   │   └── ai/
│   │       └── AIAssistant.tsx
│   │
│   ├── hooks/                  # 自定义Hooks
│   │   ├── useAuth.ts
│   │   ├── useNovel.ts
│   │   └── useWebSocket.ts
│   │
│   ├── services/               # API服务
│   │   ├── api.ts              # Axios配置
│   │   ├── authService.ts
│   │   ├── novelService.ts
│   │   ├── chapterService.ts
│   │   └── aiService.ts
│   │
│   ├── store/                  # Zustand状态管理
│   │   ├── authStore.ts
│   │   └── novelStore.ts
│   │
│   ├── router/                 # 路由配置
│   │   └── index.tsx
│   │
│   ├── types/                  # TypeScript类型定义
│   │   ├── user.ts
│   │   ├── novel.ts
│   │   └── api.ts
│   │
│   ├── utils/                  # 工具函数
│   │   ├── constants.ts
│   │   └── helpers.ts
│   │
│   └── styles/                 # 样式文件
│       └── globals.css
│
├── .env                        # 环境变量
├── .env.example
├── index.html
├── package.json
├── vite.config.ts
├── tailwind.config.ts
├── tsconfig.json
└── README.md
```

---

## 🗄️ 数据库设计

### MySQL配置信息
```
数据库名称: aiwrite_db
用户名: root
密码: 123456
端口: 3306
字符集: utf8mb4
排序规则: utf8mb4_unicode_ci
```

### 数据表设计（从Prisma迁移到MySQL）

#### 1. users (用户表)
```sql
CREATE TABLE users (
    id VARCHAR(36) PRIMARY KEY,
    email VARCHAR(255) UNIQUE NOT NULL,
    name VARCHAR(100),
    avatar VARCHAR(500),
    password VARCHAR(255),
    email_verified DATETIME,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_email (email)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```


