# 轻写 - AI辅助小说创作平台 前后端分离迁移计划

## 📋 项目概述

**原架构**：Next.js 全栈框架（前后端耦合）  
**目标架构**：React 前端 + Python FastAPI 后端（前后端分离）  
**数据库**：MySQL 8.0（后端同时支持 SQLite 便于本地快速运行）  
**预计工期**：5-7天

---

## 🎯 技术栈选型

### 前端技术栈

- React 19 + Vite
- React Router
- Axios

（如需 UI 升级可逐步引入 TailwindCSS / Shadcn / React Query / Zustand）

### 后端技术栈

- Python 3.10+
- FastAPI
- SQLAlchemy
- Pydantic
- MySQL 8.0（可选）
- JWT 认证
- httpx（豆包 API 调用）

---

## 📁 目标目录结构（落地版本）

### 后端 (backend/)

- `backend/app/main.py`：FastAPI 入口（启动时自动建表）
- `backend/app/config.py`：配置（自动读取 `backend/.env`）
- `backend/app/database.py`：数据库引擎/会话
- `backend/app/models/`：用户/小说/章节/角色/大纲/世界观 模型
- `backend/app/api/`：路由（auth/novels/chapters/characters/outlines/world-building/ai）
- `backend/app/services/`：业务逻辑层

### 前端 (frontend/)

- `frontend/src/pages/App.tsx`：落地页
- `frontend/src/pages/Login.tsx`：登录/注册
- `frontend/src/pages/Dashboard.tsx`：工作台（小说列表/创建）
- `frontend/src/pages/Novel.tsx`：小说编辑（章节/世界观/大纲/角色 + AI 续写）

---

## 🗄️ MySQL 配置（建议）

```
数据库名称: aiwrite_db
用户名: root
密码: 123456
端口: 3306
字符集: utf8mb4
```

后端连接串示例（写入 `backend/.env`）：

```env
DATABASE_URL=mysql+pymysql://root:123456@localhost:3306/aiwrite_db?charset=utf8mb4
```

